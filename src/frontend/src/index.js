import React from 'react';
import ReactDOM from 'react-dom';
import RedirectComponent from './components/RedirectComponent';

ReactDOM.render(
    <RedirectComponent />,
    document.getElementById('root')  // Assuming your html file has a div with id 'root'
);